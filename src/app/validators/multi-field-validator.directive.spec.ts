import { MultiFieldValidatorDirective } from './multi-field-validator.directive';

describe('MultiFieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiFieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
